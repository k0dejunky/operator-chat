<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release build: HTTPS only, system trust store (plus certificate
         pinning for amethyst2213.com in ChatBridge). -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>